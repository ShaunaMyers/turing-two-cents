import React from 'react';
import './Loader.css';

const Loader = ({ loading }) => {
    return (
        <section className="loader">
            <p>Loading Tips...</p>
        </section>
    )
}

export default Loader;